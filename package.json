{
  "name": "mathlet",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "registry": "tsx scripts/generate-registry.ts",
    "ssg": "tsx scripts/ssg.ts",
    "build": "npm run registry && vite build && npm run ssg",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=mathlet --commit-dirty=true"
  },
  "dependencies": {
    "katex": "^0.16.11",
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@types/jsdom": "^21.1.7",
    "@types/katex": "^0.16.7",
    "@types/node": "^22.5.0",
    "@types/three": "^0.169.0",
    "jsdom": "^25.0.0",
    "playwright": "^1.59.1",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
